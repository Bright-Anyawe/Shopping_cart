
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import App from "../Layout/App";


describe("App component", () => {
  

  it("should render the correct headiing", () => {
    expect(false).toBe(false);
  });
});